name: 📚 Publish Flutter DartDoc to GitHub Pages

on:
  push:
    branches:
      - main

jobs:
  flutter-docs:
    name: Generate & Deploy Flutter Docs
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repo
        uses: actions/checkout@v3

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.22.1' # Use your package's Flutter SDK version
          channel: stable

      - name: Get Flutter Dependencies
        run: flutter pub get

      - name: Generate dartdoc
        run: flutter pub global activate dartdoc && dartdoc --output docs

      - name: Deploy to GitHub Pages
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs
          publish_branch: gh-pages
          force_orphan: true
